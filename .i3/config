# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

new_window none
new_float 1pixel
hide_edge_borders both

exec dropbox start
exec gnome-settings-daemon
exec feh --bg-tile ~/.i3/desktop_solarized.png
# apt-get install kupfer
# exec kupfer

for_window [class="feh"] floating enable
for_window [class="Firefox" instance="^Download"] floating enable
for_window [class="^GIMP"] floating enable
for_window [class="MPlayer"] floating enable
for_window [class="Remmina"] floating enable
for_window [class="Skype"] floating enable
for_window [class="(Steam|hl_linux)"] floating enable

# colors                 border background text indicator
client.focused           #073642 #073642 #eee8d5 #90A959
client.focused_inactive  #586e75 #002b36 #586e75 #303030
client.unfocused         #002b36 #002b36 #586e75 #303030
client.urgent            #C86F70 #fdf6e3 #eee8d5
client.background #002b36 

# workspace "[1]" output CRT1
# workspace "[2]" output CRT1
# workspace "[3]" output CRT1
# workspace "[4]" output LVDS
# workspace "[5]" output LVDS
# workspace "[6]" output LVDS

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# see full charset using `xfd -fa terminus`
# get font strings using `xfontsel`
# font for window titles. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font pango:Terminus 8px
# font -*-terminus-medium-r-*-*-*-120-75-75-*-*-iso8859-15
# font -*-ohsnap-medium-*-*-*-12-*-*-*-*-*-*-*

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#  lxterminal Background color #00141A
bindsym $mod+Return exec lxterminal
# bindsym $mod+Return exec terminology
bindsym $mod+Shift+T exec i3-sensible-terminal
bindsym $mod+Shift+F exec firefox
bindsym $mod+Shift+S exec surf https://www.duckduckgo.com
bindsym $mod+Shift+I exec google-chrome --incognito

# kill focused window
bindsym $mod+Shift+Q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -b -fn '-*-terminus-medium-r-*-*-*-120-75-75-*-*-iso8859-15'

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# scroll through workspaces
bindsym Control+$mod+Right workspace next_on_output
bindsym Control+$mod+L workspace next_on_output
bindsym Control+$mod+Left workspace prev_on_output
bindsym Control+$mod+H workspace prev_on_output

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horiZontal orientation
bindsym $mod+z split h
# split in Vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace ⠁
bindsym $mod+2 workspace ⠉
bindsym $mod+3 workspace ⠋
bindsym $mod+4 workspace ⠛
bindsym $mod+5 workspace ⠟
bindsym $mod+6 workspace ⠿
bindsym $mod+7 workspace ⡿
bindsym $mod+8 workspace ⣿
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 0

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace ⠁
bindsym $mod+Shift+quotedbl move container to workspace ⠉
bindsym $mod+Shift+periodcentered move container to workspace ⠋
bindsym $mod+Shift+dollar move container to workspace ⠛
bindsym $mod+Shift+percent move container to workspace ⠟
bindsym $mod+Shift+ampersand move container to workspace ⠿
bindsym $mod+Shift+slash move container to workspace ⡿
bindsym $mod+Shift+parenleft move container to workspace ⣿
bindsym $mod+Shift+parenright move container to workspace 9
bindsym $mod+Shift+equal move container to workspace 0

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym k resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        # apt-get install xfonts-terminus
        # font -*-terminus-medium-r-*-*-*-120-75-75-*-*-iso8859-15
        font pango:Terminus 8px
        # http://askubuntu.com/questions/135437/install-artwiz-fonts-in-ubuntu-12-04
        # font -*-ohsnap-medium-*-*-*-12-*-*-*-*-*-*-*
        status_command i3status
        position top
        # mode hide
        colors {
          background #002b36
          statusline #586e75
          separator #073642
          #                  border background text
          focused_workspace  #fdf6e3 #eee8d5 #586e75
          active_workspace   #586e75 #073642 #586e75
          inactive_workspace #073642 #073642 #586e75
          urgent_workspace   #fdf6e3 #073642 #586e75
        }
}
